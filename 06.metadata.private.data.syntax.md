#### Metadata Private Data Syntax

~~~~~
metadata_private_data( sz ) {
    for ( i = 0; i < sz; i++ )
        @@metadata_private_data_payload_byte[ i ]                              f(8)
}
~~~~~
{:.syntax }
